{% extends 'base.html.twig' %}
{% block body %}

<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>#</th>
        <th>Nazwa sprawdzianu</th>
      </tr>
    </thead>
    <tbody>
    {% for exam in exams %}
      <tr>
        <td>{{ exam.id }}</td>
        <td>{{ exam.name }}</td>
        <td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
{{ form(add_exam) }}
{% endblock %}